The need of the task was to create an html file that contains h1 to h3 tags. using them we had to give short introduction about us so I did this by using h1 tag inside it I wrote my name because search engine gives priority to whatever we write inside these tags so we always put our important content in it which represents our page. after that I used the paragraph tag for introducing my self. as per the instruction I used the boilerplate code in VS-Code if just write '!' exclamation mark it generates an html emit. 